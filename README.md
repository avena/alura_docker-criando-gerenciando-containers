## Docker_Containers_Imagens_Volumes_e_Networks_Treinaweb_2023


coloca linha abaixo em formato markdownd de code

```bash
docker image build -t fernandoavena/ubuntucomvim:latest .
```

Lista as Imagens
```bash
docker images
```

Remove todos os dados de build em cache (incluindo imagens não utilizadas)
```bash
docker builder prune -a
```